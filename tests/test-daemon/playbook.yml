---
- hosts: all
  vars:
    program:
      binary: tests/fixtures/hellod
      name: hello
      description: Keeps on saying hello
  roles:
    - role: uhlig-it.ansible-role-simple-systemd-service
      become: true
